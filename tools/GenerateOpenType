##!/usr/bin/env fontforge
# $Id: GenerateOpenType,v 1.1 2008-03-24 11:59:49 Stevan_White Exp $
#
# Convert fonts from FontForge's native SFD format to OpenType format
# First auto-hints whole font

scriptname=$argv[0];

if ( $argc > 1 )
    i = 1;
    while ( i < $argc )
	Open( $argv[i] )
	otfile = $fontname + ".otf"
	Print( "Generating OpenType file ", otfile )
	SelectAll()
	AutoHint()
	Generate( otfile )
	Close()
	i++
    endloop
else
    Print( "Usage: ", scriptname, " font.sfd [font.sfd ...]" )
endif
